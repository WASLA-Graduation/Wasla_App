import 'package:flutter/material.dart';

class GymPackagesView extends StatelessWidget {
  const GymPackagesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('GymPackagesView'),
    );
  }
}
