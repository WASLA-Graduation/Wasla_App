import 'package:flutter/material.dart';

class GymDashboardView extends StatelessWidget {
  const GymDashboardView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("GymDashboardView"));
  }
}
