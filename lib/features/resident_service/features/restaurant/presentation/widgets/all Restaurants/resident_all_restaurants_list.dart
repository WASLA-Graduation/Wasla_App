import 'package:flutter/material.dart';

class ResidentAllRestaurantsList extends StatelessWidget {
  const ResidentAllRestaurantsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}