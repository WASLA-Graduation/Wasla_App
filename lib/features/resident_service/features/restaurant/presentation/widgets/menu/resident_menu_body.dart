import 'package:flutter/material.dart';

class ResidentMenuBody extends StatelessWidget {
  const ResidentMenuBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Resident Menu Body'));
  }
}
