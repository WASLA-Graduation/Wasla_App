import 'package:flutter/widgets.dart';

class ResidentAllTechinicalsList extends StatelessWidget {
  const ResidentAllTechinicalsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
